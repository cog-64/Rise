/**
 * tests for the core package to be run by velocity;
 * @see {@link https://velocity.readme.io/docs/jasmine-testing-a-meteor-package}
 */

describe('Rise', function () {
    it('is defined', function () {
        expect(it).toBeDefined();
    })
})